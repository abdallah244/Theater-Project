<!--
Audience: UI/UX Designer
Goal: Define what to design and how it should behave.
Note: Keep scope strictly aligned with project spec.
-->

# UI/UX Spec — تصميم وتجربة الاستخدام

**المشروع:** موقع ويب لحجز مقاعد عروض مسرح الأطفال/السيرك (Mobile-first)

**هدف الوثيقة:** تحديد الشاشات المطلوبة، وتدفق المستخدم، وسلوك العناصر الأساسية (خصوصًا خريطة المقاعد) بحيث يكون التصميم قابل للتنفيذ مباشرة.

---

## 1) مبادئ التصميم

- Mobile-first: كل شيء يبدأ من شاشة موبايل.
- البساطة: أقل عدد خطوات لإتمام الحجز.
- الوضوح: حالة المقعد مفهومة بالألوان والـ legend.
- منع الأخطاء: تقليل فرص اختيار مقعد غير صالح + توضيح الرسائل.

**ألوان حالات المقعد (ثابتة بالمواصفات):**

- 🟢 متاح
- 🔴 محجوز
- ⚫ مخفي

---

## 2) خريطة المعلومات (IA)

### Public

- قائمة العروض
- صفحة العرض + خريطة المقاعد
- بيانات وليّ الأمر
- تأكيد الحجز / كود الحجز
- (اختياري) متابعة الحجز

### Admin

- تسجيل الدخول
- إدارة العروض
- إدارة عرض محدد:
  - فتح/قفل الحجز
  - التسعير حسب الصف
  - تعديل التخطيط (إخفاء صفوف/مقاعد)
- متابعة الحجوزات

---

## 3) تدفقات المستخدم (User Flows)

### 3.1 حجز مقاعد (Public)

1. المستخدم يفتح الموقع → يرى قائمة العروض.
2. يختار عرض + تاريخ/ميعاد.
3. تظهر خريطة المقاعد:
   - المستخدم يختار أكثر من كرسي.
   - يظهر ملخص المقاعد + إجمالي السعر.
4. ينتقل لإدخال بيانات وليّ الأمر.
5. تأكيد الحجز.
6. يظهر كود الحجز + الحالة.
7. (اختياري) الدفع:
   - مسار دفع أونلاين (لو موجود)
   - أو تعليمات دفع يدوي + رقم مرجعي

### 3.2 إدارة العرض (Admin)

1. Admin Login.
2. فتح قائمة العروض.
3. اختيار عرض:
   - فتح/قفل الحجز.
   - تعديل الأسعار لكل Row.
   - إخفاء/إظهار صفوف أو كراسي.
4. متابعة الحجوزات + (اختياري) تأكيد الدفع اليدوي.

---

## 4) الشاشات المطلوبة (Screens)

> المطلوب Wireframes + تصميم نهائي بعد موافقة.

### S1 — قائمة العروض (Shows List)

**محتوى:**

- Card لكل عرض: اسم العرض + التاريخ/الوقت + زر “احجز الآن”.
- حالة الحجز: OPEN/CLOSED (لو CLOSED يظهر “الحجز مغلق”).

**حالات:**

- Loading
- Empty list (لا توجد عروض)
- Error (فشل تحميل)

---

### S2 — صفحة العرض + خريطة المقاعد

**محتوى رئيسي:**

- عنوان العرض + التاريخ/الوقت.
- Legend لحالات المقاعد (🟢/🔴/⚫).
- خريطة المقاعد (Rows مثل A–O) + أرقام المقاعد.
- ملخص أسفل الخريطة:
  - المقاعد المختارة
  - السعر الإجمالي
  - زر “متابعة” أو “تأكيد البيانات”

**سلوك خريطة المقاعد:**

- المقعد المتاح: قابل للنقر (Select).
- المقعد المحجوز: غير قابل للنقر.
- المقعد المخفي: غير قابل للنقر + لا يُحسب ولا يظهر كمقعد متاح.
- اختيار متعدد: ضغط مرة يختار، مرة أخرى يلغي.
- عند غلق الحجز (CLOSED):
  - عرض رسالة واضحة “الحجز مغلق لهذا العرض”.
  - تعطيل الاختيار وزر المتابعة.

**حالات:**

- Loading للخريطة
- Error (مثلاً تعذر تحميل المقاعد)

---

### S3 — بيانات وليّ الأمر

**حقول:**

- اسم وليّ الأمر (Required)
- رقم الموبايل (Required)
- عدد الأطفال (Required)

**محتوى إضافي:**

- ملخص الحجز: العرض + المقاعد المختارة + الإجمالي.
- زر “تأكيد الحجز”.

**Validation/Errors:**

- رسائل قصيرة أسفل الحقول.
- لو حصل تعارض مقاعد بعد الإرسال (Seat already reserved):
  - رسالة واضحة
  - زر “العودة للمقاعد”

---

### S4 — تأكيد الحجز

**محتوى:**

- رسالة نجاح.
- كود الحجز.
- تفاصيل: العرض/التاريخ/المقاعد/الإجمالي.
- حالة الدفع:
  - Paid
  - Pending payment (لو مسار يدوي)

**لو الدفع اليدوي:**

- تعليمات الدفع + رقم مرجعي واضح.

---

### S5 — Admin Login

**محتوى:**

- Username/Password أو Email/Password (حسب backend).
- زر دخول.
- حالات: Loading/Error.

---

### S6 — Admin: قائمة العروض

**محتوى:**

- List/Table للعروض.
- إنشاء عرض جديد.
- فتح صفحة إدارة عرض.

---

### S7 — Admin: إدارة عرض

**أقسام:**

1. فتح/قفل الحجز (Toggle واضح)
2. التسعير حسب الصف (Row pricing)
3. تعديل التخطيط (Layout):
   - إخفاء/إظهار صف كامل
   - إخفاء/إظهار كرسي محدد
4. الحجوزات (Link أو Tab)

**ملاحظة UX مهمة:**

- أي تعديل (Pricing/Layout/Status) لازم يوضح “تم الحفظ” أو “فشل الحفظ”.

---

### S8 — Admin: الحجوزات

**محتوى:**

- Table: كود الحجز، وليّ الأمر، الموبايل، عدد الأطفال، المقاعد، الإجمالي، حالة الدفع/الحجز.
- (اختياري للمسار اليدوي) Action: “Mark Paid”.

---

## 5) النصوص (Microcopy) المقترحة

- عند غلق الحجز: “الحجز مغلق حاليًا لهذا العرض.”
- عند محاولة اختيار محجوز: “هذا المقعد محجوز.”
- عند تعارض المقاعد بعد الإرسال: “بعض المقاعد أصبحت محجوزة، من فضلك اختر مقاعد أخرى.”
- نجاح الحجز: “تم تأكيد حجزك بنجاح.”

---

## 6) حالات عامة يجب تصميمها

- Loading skeleton بسيط.
- Empty states.
- Error states مع زر “إعادة المحاولة”.
- Disabled states (خصوصًا عند غلق الحجز).

---

## 7) مخرجات الـ UI/UX المطلوبة للتسليم

- Wireframes لكل الشاشات S1–S8.
- Prototype (Click-through) لرحلة الحجز الأساسية.
- Spec للتفاعلات في Seat Map (Select/unselect/disabled).
- Responsive layouts للموبايل + نظرة سريعة على Tablet/Desktop.

---

## 8) حدود النطاق (Scope Guard)

- لا يتم إضافة صفحات/Features خارج المذكور في هذه الوثيقة وملف [report.md](../report.md).
- أي تحسينات (Zoom/Pan متقدم، فلاتر، حسابات مستخدمين…) تعتبر خارج الـ MVP إلا بطلب صريح.
